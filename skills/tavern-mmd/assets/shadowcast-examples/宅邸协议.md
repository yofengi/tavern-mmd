# 宅邸状态面板 生成协议（模型侧 · 蓝灯常驻条目）

每次回复正文结束后，**另起一行**用单个 `<g3>...</g3>` 块输出当前状态全量快照。**固态字段每轮必出**（即使没变也照原值重出）；**情境字段仅在对应情境那轮输出**。

> 由雷达法转换而来：取消散落正文的 `[键=值]` 信标与信标转换器，改单块结构化输出。tooltip 说明（效果/成因等）**内联进对应字段**，不再散落正文。

## 输出格式
```
<g3>
name=白雪凝;time=深秋十月 黄昏;loc=云岚宅邸-主楼-二层书房;look=月白旗袍，发髻微松，眼角含春;mood=垂眸抿唇，指尖轻绞衣角;manor=回廊尽头两名女仆低声议论着昨夜的动静;fall=35/100;turn=5;log=1.初见于宅邸门前|2.书房密谈|3.察觉异样|4.暂无;event=1/3|暂无
;opt=温柔安抚|上前轻握她的手，柔声问她在烦扰什么
</g3>
```

- 字段间用 `;` 分隔，键值用 `=`。
- **multi 字段**每条目重复一次该键（如 `opt=...;opt=...`）。
- 值里**禁止** `< > = ;` 与换行。各字段内部分隔符见下表。

## 字段表

| 键名 | 含义 | 类型 | 格式 | 示例 | 代谢 | 别名 |
|---|---|---|---|---|---|---|
| `name` | 姓名 | text | 当前交互对象角色名 | `白雪凝` | 固态（继承） | `姓名`／`交互对象` |
| `time` | 时间 | time | 年月日 时段（清晨/上午/中午/下午/黄昏/夜间/深夜） | `深秋十月 黄昏` | 固态（继承） | `时间` |
| `loc` | 地点 | path | 区域-具体位置（连字符分层） | `云岚宅邸-主楼-二层书房` | 固态（继承） | `地点`／`位置` |
| `look` | 外貌 | text | 当前衣着状态及容貌特写 | `月白旗袍，发髻微松，眼角含春` | 固态（继承） | `外貌`／`衣着` |
| `mood` | 神态 | text | 当前表情或动作 | `垂眸抿唇，指尖轻绞衣角` | 固态（继承） | `神态`／`表情` |
| `manor` | 宅邸掠影 | text | 宅邸内女仆动态/流言/画面特写（离开宅邸则换为环境描写） | `回廊尽头两名女仆低声议论着昨夜的动静` | 固态（继承） | `宅邸掠影`／`掠影`／`环境` |
| `fall` | 堕落值 | bar | 当前/100（交互对象非白雪凝时键名语义为好感度） | `35/100` | 固态（继承） | `堕落值`／`好感度`／`好感` |
| `doubt` | 怀疑度 | bar | 当前/100（条件触发常驻：起疑后解锁，解锁后每轮必出，误会解除前不停） | `20/100` | 情境（用完即焚） | `怀疑度`／`疑心` |
| `turn` | 回复轮次 | turn | 数字（每轮+1） | `5` | 固态（继承） | `回复轮次`／`轮次` |
| `log` | 事件记录 | lines | N.内容|N.内容|...（每条≤60字，最多4条，超4条删最旧上移） | `1.初见于宅邸门前|2.书房密谈|3.察觉异样|4.暂无` | 固态（继承） | `事件记录`／`记录` |
| `event` | 事件总结 | summary | N/3|内容（N=1或2填 暂无；N=3填约150字总结，下轮重置1） | `1/3|暂无` | 固态（继承） | `事件总结`／`总结` |
| `lewd` | 色情特写 | kvlist | 部位:描写,部位:描写（仅色情互动那轮输出，脱离立即停） | `蜜穴:微微开合渗出晶莹,体位:背入` | 情境（用完即焚） | `色情特写`／`特写`／`淫靡` |
| `opt` | 选项 | option | 简介|内容（每个选项一条 opt=；触发性爱在简介前加 ♥） | `温柔安抚|上前轻握她的手，柔声问她在烦扰什么` | 情境（用完即焚） | `选项` |

## 分隔符约定

| 层级 | 符号 | 用于 |
|---|---|---|
| 字段间 | `;` | 所有字段 |
| 键值 | `=` | 所有字段 |
| 主分割 | `\|` | bar成因/level经验/kvlist名↔说明/enemy各段/opt简介↔内容/bag类↔物品/lines各条 |
| 列表项 | `,` | stats各属性/kvlist各条 |
| 次级项 | `/`或`、` | bag同类物品/enemy属性组 |
| 标签值 | `:` | stats`属性:值`/kvlist`槽位:名`/enemy属性`AC:14` |

## 铁律

- **推荐标准键名**（表第一列）；引擎对别名归一化容错，但标准键名最稳。
- **固态字段每轮必出**（面板无跨气泡状态、靠每轮全量，缺了引擎才向历史折叠兜底）。
- **情境字段 `doubt`/`lewd`/`opt`：只在该情境那轮输出，情境结束立即停止**（不每轮重复、不在情境外保留——面板随之消失，是防高刺激信息污染上下文的代谢机制，不是 bug）。
- 选项：3~4 个差异化走向；可触发性爱在简介前加 `♥`；简介≤10字、内容约70~80字含动作言语。
- 只吐数据块，不解释、不加排版标记。