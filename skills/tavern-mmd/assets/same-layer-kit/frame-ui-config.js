/* Author options for the Frame UI. These switches do not grant native permissions. */
globalThis.MmdSameLayerUIConfig = Object.freeze({
  theme: 'dark',
  modules: Object.freeze({
    messageActions: true, models: true, conversations: true, persona: true,
    supplement: true, instructions: true, settings: true, sharing: true,
    navigation: true, more: true, game: true
  })
});
