---
description: 查看 tavern-mmd 全部指令说明（平台指令×任务指令用法与组合示例）
---

向用户输出以下帮助内容（保留表格与组合示例，可按对话语境补充说明）：

# tavern-mmd 指令帮助

本套指令分两个维度：**平台指令**设定目标酒馆平台，**任务指令**执行具体制作。两者自由组合，例如先 `/oldmmd` 再 `/beautify` = 给旧版MMD做美化。

## 平台指令（设定本次制作的目标平台）

| 指令 | 平台 | 关键特点 |
|---|---|---|
| `/mmd` | 当前MMD（魅魔岛/sexyai.top） | 支持`<script>`，其余限制按旧版保守处理 |
| `/oldmmd` | 旧版MMD | 禁`<script>`（用img onerror点火器）、ES5 only、正则手填≤30条 |
| `/st` | 本地酒馆SillyTavern | 无限制：script/ES6+可用、正则json直接导入、世界书全字段 |

平台会写入项目 main.md；未设定平台时，任务指令会先弹窗询问。

## 任务指令（执行具体制作）

| 指令 | 用途 | 适合场景 |
|---|---|---|
| `/cardplan` | 角色卡标准流程：弹窗快问快答→写plan.md→执行 | 需求明确的中小型卡 |
| `/cardplanmax` | 角色卡深度共创：大段开放讨论→方案收敛→分节设计确认→分段执行 | 从零共创、复杂卡、想法还模糊 |
| `/worldbook` | 制作世界书：条目规划表确认→逐条写作→输出可导入json | 单独做世界书或为已有卡补世界书 |
| `/beautify` | 制作美化：全局美化（界面换肤）或状态栏（消息末尾数据面板） | 单独做美化组件 |
| `/helpmmd` | 显示本帮助 | — |

## 常用组合示例

- `/oldmmd` → `/beautify` 选状态栏：旧版MMD状态栏（KV V4.0三段正则方案，产出手填清单）
- `/st` → `/worldbook`：本地酒馆世界书（可用全部字段，产出json直接导入）
- `/mmd` → `/cardplanmax`：从零共创一张当前MMD完整角色卡
- 不带指令直接聊（如"给旧版MMD做个状态栏"）也会自动触发 tavern-mmd skill

## 产出物

| 产出 | 本地酒馆 | MMD（新旧同） |
|---|---|---|
| 角色卡 | chara_card_v3 json | 同左（MMD支持json导入） |
| 世界书 | 世界书 json | 同左 |
| 正则 | 正则脚本 json | 手填清单 .md（逐条复制粘贴到平台UI） |

每个项目使用独立文件夹（main.md / plan.md / 资料 / 工作 / output 五件套），中断后新会话读 main.md+plan.md 可无缝续作。
